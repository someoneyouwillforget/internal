return {
    Default = {
        -- Fonts (Clean & Bold)
        TextFont = Enum.Font.SourceSansBold,
        TextColor = Color3.fromRGB(255, 255, 255),
        
        -- Backgrounds
        Background = Color3.fromRGB(10, 10, 10), -- Darkest main frame
        Topbar = Color3.fromRGB(20, 20, 20),    -- Slightly lighter header
        ElementColor = Color3.fromRGB(25, 25, 25), -- Color of toggle/button boxes
        
        -- The "Border Tabs" & Accents
        Accent = Color3.fromRGB(255, 255, 255),
        BorderColor = Color3.fromRGB(255, 255, 255), -- This creates the white boxes
        
        -- Rounding
        Rounding = UDim.new(0, 14)
    }
}
